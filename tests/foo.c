int entry (int n, int m, int x, int y){
  int res = 0;
  if(0) {
    res += n;
    res -= m;
    res += x*y;
    res++;
  } else {
    res += n;
    res -= m;
    res += x*y;
    res++;
  }
  return res;
}

